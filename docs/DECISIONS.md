# Decisiones — CubiOps

## Astro en lugar de un panel

La web es de captación: se sirve estática, no compite con el puerto 8080, y el JS se limita al menú y al formulario.

## Contacto sin falso positivo

Si faltan `SMTP_HOST`, `SMTP_USER`, `SMTP_PASS` o `CONTACT_TO`, el formulario no simula un envío correcto.

## 2026-08-31 — Contexto en el repo

AGENTS.md y `docs/` para continuar sesiones desde Windows o iPhone.
